

Single change: in `src/components/BeltBadge.tsx`, update the `Azul` entry's `bg` from `#134C73` to `#4285F4`. Text color stays white (good contrast).

No other belt colors, components, or files affected.

**File to edit:**
- `src/components/BeltBadge.tsx` — line for `Azul`: change `bg: '#134C73'` → `bg: '#4285F4'`

